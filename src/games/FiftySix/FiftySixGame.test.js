import { Bid,PlayCard} from './modules/moves.js'
import { DecideWinner } from './modules/move_helpers.js'
it('should determine the correct next player(winner of a round)',()=>{
      const G={
trump:
currentCards:[

]
      }
      winner=DecideWinner(G)
});
// TODO test if the correct next player is being set
// TODO check for all instances where the next turn is being determined.